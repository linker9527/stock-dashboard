{
  "name": "stock-dashboard-workers",
  "version": "1.0.0",
  "description": "Stock dashboard API with primary/backup data sources",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
